import math
###
###
###

#Grocery Lists
groceries = ['tomatoes', 'butter', 'eggs', 'miracle whip' ]

'returns above list'
print(groceries)

'returns 2nd item in the list'
print(groceries[1])

'returns last three items'
print(groceries[-3:])

'returns 2nd and 3rd elements in the list'
print(groceries[1:3])
print(groceries[0:3])


'returns last two elements of the list'
print(groceries[-2:])

'everything before eggs'
print(groceries[:-2])

'Extend allows you to add individual items to the end of lists'
groceries.extend(["cheese", "turkey"])

#Oh wait, we have forgotten a few items, and we
#actually want ice cream, not miracle whip
groceries[3] = "Ice Cream"

print(groceries)

'how long is your list of groceries'
groceries.remove("Chocolate bar")

'Dont want cheese or butter'
groceries.remove("cheese")

'Adds chocolate bars at index position 1'
groceries.insert(1, "chocolate bar")

'Do we have butter on the list?'
print(groceries.index("butter"))

groceries.sort()
print(groceries)
###
###
###

Sample_set2 = ['10', 'sword', 'bassfish', '3.14', '44', 'logic']

'returns how long the list is'
how_long = len(Sample_set2)
print(how_long)



random_numbers_list = [2, 12, 1.01,-4, 12, 222,] 

'min, max '
print(min(random_numbers_list))
print(max(random_numbers_list))


'returns how many items are in the list'
how_many_items = len(random_numbers_list)
print("we have " + str(how_many_items) + " items in the list")



bax = print([len(i) for i in Sample_set2])
print(bax)

def Sample_set2(words):
  ctr = 0

  for word in words:
    if len(word) > 1 and word[0] == word[-1]:
      ctr += 1
  return ctr

print(Sample_set2(['10', 'sword', 'bastinb', 'appleapllea', 'bassfish', '3.14', '44', 'logic']))


name = input("Hi! What’s your name?")
print("Nice to meet you" + name + "!")
